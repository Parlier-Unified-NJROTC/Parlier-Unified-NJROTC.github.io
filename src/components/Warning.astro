
            <!-- Exit Warning Modal -->
            <div id="exit-warning" class="exit-modal">
                <div class="exit-modal-content">
                    <div class="warning-icon"><svg height="50px" width="100px" version="1.1" id="_x32_" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 512 512" xml:space="preserve" fill="#000000"><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier"> <style type="text/css"> .st0{fill:#e6b220;} </style> <g> <path class="st0" d="M238.301,346.393c0.598,3.938,2.563,7.725,5.903,11.359c3.313,3.626,7.252,5.447,11.796,5.447 c10.592,0,16.486-5.608,17.691-16.806l35.398-271.98c0.607-4.823,0.911-11.636,0.911-20.415c0-13.618-4.679-26.013-14.065-37.22 C286.558,5.59,273.244,0,255.999,0c-17.868,0-31.317,5.742-40.389,17.226c-9.073,11.206-13.61,23.459-13.61,36.773 c0,8.172,0.285,14.976,0.892,20.415L238.301,346.393z"></path> <path class="st0" d="M295.033,418.065c-10.288-10.287-23.307-15.44-39.034-15.44c-15.422,0-28.441,5.314-39.032,15.896 c-10.591,10.591-15.877,23.441-15.877,38.569c0,14.52,5.286,27.379,15.877,38.577C227.558,506.562,240.578,512,255.999,512 c15.423,0,28.424-5.286,39.034-15.886c10.574-10.574,15.877-23.593,15.877-39.024C310.91,441.658,305.607,428.656,295.033,418.065z "></path> </g> </g></svg></div>
                    <h3>Leaving Our NJROTC Site</h3>
                    <p>You are about to leave the official website. External links may not follow the same security protocols or ad policy.</p>
                    <div class="button-group">
                        <button id="cancel-exit" class="exit-btn cancel-btn">Stay Here</button>
                        <button id="confirm-exit" class="exit-btn confirm-btn">Continue Anyway</button>
                    </div>
                </div>
            </div>
    
    <style>
        /* Warn Section Base */
        .Warn-section {
            padding: 6rem 2rem;
            background: #000000;
            position: relative;
            z-index: 1;
            overflow: hidden;
            font-family: 'Montserrat', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        
        .Warn-section::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgb(2 60 113 / 40%) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgb(2 60 113 / 40%) 0%, transparent 50%);
            z-index: -1;
        }
        #main-content {
            width: 100%;
        }

        /* Exit Warning Modal */
        .exit-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s ease;
        }

        .exit-modal-content {
            background: linear-gradient(135deg, rgba(10, 10, 15, 0.95), rgba(2, 60, 113, 0.2));
            border: 2px solid #e6b220;
            border-radius: 16px;
            padding: 40px;
            max-width: 500px;
            width: 90%;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.7);
            position: relative;
            overflow: hidden;
        }

        .exit-modal-content::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, rgba(2, 60, 113, 0.3), rgba(230, 178, 32, 0.1));
            opacity: 0.3;
            z-index: -1;
        }

        .warning-icon {
            font-size: 48px;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }

        .exit-modal-content h3 {
            color: #e6b220;
            font-size: 28px;
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .exit-modal-content p {
            color: #fafaf5;
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .button-group {
            display: flex;
            gap: 20px;
            justify-content: center;
        }

        .exit-btn {
            padding: 12px 30px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Montserrat', sans-serif;
            min-width: 150px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .cancel-btn {
            background: transparent;
            color: #e6b220;
            border: 2px solid #e6b220;
        }

        .cancel-btn:hover {
            background: rgba(230, 178, 32, 0.1);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(230, 178, 32, 0.2);
        }

        .confirm-btn {
            background: linear-gradient(135deg, #e6b220, #d4a41c);
            color: #000;
            border: 2px solid #e6b220;
        }

        .confirm-btn:hover {
            background: linear-gradient(135deg, #d4a41c, #c29518);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(230, 178, 32, 0.3);
        }

        /* External link badge (optional) */
        .external-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(230, 178, 32, 0.2);
            color: #e6b220;
            padding: 4px 10px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
            border: 1px solid rgba(230, 178, 32, 0.5);
            z-index: 2;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        @media (max-width: 768px) {
            .Warn-section {
                padding: 4rem 1.5rem;
            }

            .button-group {
                flex-direction: column;
                gap: 15px;
            }

            .exit-btn {
                min-width: 100%;
            }

            .exit-modal-content {
                padding: 30px 20px;
            }

            .exit-modal-content h3 {
                font-size: 24px;
            }
        }

        @media (max-width: 480px) {
            .Warn-section {
                padding: 3rem 1rem;
            }
        }
    </style>

    <script lang="js">
        document.addEventListener('DOMContentLoaded', function() {
            const exitModal = document.getElementById('exit-warning');
            const cancelExitBtn = document.getElementById('cancel-exit');
            const confirmExitBtn = document.getElementById('confirm-exit');
            let pendingUrl = '';
            

            function initializeExitWarning() {
                document.querySelectorAll('a').forEach(link => {
                    const href = link.getAttribute('href');
                    
                    if (!href || href.startsWith('#') || href.startsWith('/') || 
                        href.startsWith(window.location.origin) || 
                        href.startsWith('javascript:') ||
                        link.classList.contains('no-exit-warning')) {
                        return;
                    }

                    try {
                        const url = new URL(href);
                        if (url.origin !== window.location.origin) {
                            link.addEventListener('click', function(e) {
                                e.preventDefault();
                                pendingUrl = href;
                                exitModal.style.display = 'flex';
                            });
                        }
                    } catch (error) {
                    }
                });
            }
            
            initializeExitWarning();
            cancelExitBtn.addEventListener('click', function() {
                exitModal.style.display = 'none';
                pendingUrl = '';
            });
            confirmExitBtn.addEventListener('click', function() {
                if (pendingUrl) {
                    exitModal.style.display = 'none';
                    window.open(pendingUrl, '_blank');
                    pendingUrl = '';
                }
            });
            
            exitModal.addEventListener('click', function(e) {
                if (e.target === exitModal) {
                    exitModal.style.display = 'none';
                    pendingUrl = '';
                }
            });

            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape' && exitModal.style.display === 'flex') {
                    exitModal.style.display = 'none';
                    pendingUrl = '';
                }
            });
            
            const observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                    if (mutation.addedNodes.length) {
                        initializeExitWarning();
                    }
                });
            });
            
            observer.observe(document.body, { childList: true, subtree: true });
        });
    </script>